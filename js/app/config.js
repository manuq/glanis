define([],
function() {
    var config = {
        frameWidth: 312, // 300px width + 2*6px border
        frameHeight: 312,
        space: 60,
        maxDuration: 1500,
        brushSize: 5,
        eraserSize: 15,
    };

    return config;

});
