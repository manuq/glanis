define([],
function() {
    var config = {
        frameWidth: 312, // 300px width + 2*6px border
        frameHeight: 312, // 390px width + 2*6px border
        space: 60,
        maxDuration: 500,
        brushSize: 5,
        eraserSize: 15,
    };

    return config;

});
